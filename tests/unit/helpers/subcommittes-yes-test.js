import { subcommittesYes } from 'congress-code/helpers/subcommittes-yes';
import { module, test } from 'qunit';

module('Unit | Helper | subcommittes yes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subcommittesYes([42]);
  assert.ok(result);
});
